# Mastodon — Network Visualization Hub

**Char target**: ≤500

---

Shipped today: Network Visualization Hub.

Live + free on resistancezero.com:
→ Modbus RTU (RS-485 serial, modem-V21 register)
→ Modbus TCP (MBAP header chip visibly larger than payload — overhead made visible)
→ BACnet/IP (BVLC tunnel rendered as scan-line shroud at packet head)
→ OPC-UA (subscription model with always-on encryption shroud)
→ Compare mode (4-panel side-by-side)

Audit-enforced anti-monotony: no two protocols in the same lane share >2 timbre fields. Each has its own audio + visual signature.

Canvas 2D + Web Audio. Industrial register, not consumer techno.

resistancezero.com/network-visualization-hub.html

---

(~496 chars)
